name: loglens
description: A flexible, extensible logging utility with dynamic modules/layers, persistence, and a simple console UI.
version: 0.5.0

homepage: https://github.com/InsNeed/log_lens
repository: https://github.com/InsNeed/log_lens
issue_tracker: https://github.com/InsNeed/log_lens/issues
topics:
  - logging
  - console
  - debugging

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  logger: ^2.0.2+1

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
